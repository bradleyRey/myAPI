'use strict'

const request = require('request')
const schema = require('./spotifySchema')
const mongoose = require('./mongoose')
const hash = require('bcrypt')

exports.addFavorites = function addFavorites(req,res,next) {
   
}